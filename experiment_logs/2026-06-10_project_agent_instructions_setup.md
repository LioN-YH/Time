# 项目级 agent 工作规范建立日志

## 日志信息

- 日志日期：2026-06-10 02:34:32 CST
- 工作目录：`/home/shiyuhong/Time`
- 相关文件：
  - `AGENTS.md`
  - `experiment_logs/README.md`

## 目的

将后续协作中的长期要求写入项目级指令文件，使未来在 `/home/shiyuhong/Time` 工作区内工作的 agent 能够稳定理解并遵守这些规则。

## 背景

用户提出希望后续实验过程能够持续记录日志，并补充了两个额外要求：

1. 如果过程中需要写计划、方案或执行步骤，必须使用中文。
2. 写代码时需要添加足够量的中文注释，便于后续审查和理解；注释至少应覆盖文件功能、函数功能和复杂逻辑附近的实现说明。

为了让这些要求便于后续 agent 理解，需要将口语化要求整理成明确、可执行、可检查的项目级规范。

## 操作

1. 在 `/home/shiyuhong/Time` 下新建 `AGENTS.md`。
2. 将用户要求整理为四类规范：
   - 实验日志规范
   - 计划和方案表达规范
   - 代码注释规范
   - 协作偏好
3. 明确实验日志文件名、正文语言、精确时间、必备章节和总览表更新要求。
4. 明确后续计划、方案、实验设计和阶段性总结应使用中文。
5. 明确新增或修改代码时需要添加中文注释，并说明注释应覆盖的范围。
6. 按照新规范，为本次规范建立工作新增实验日志。
7. 更新 `experiment_logs/README.md` 中的总览追踪表。

## 结果

已新增项目级指令文件：

- `AGENTS.md`

该文件已记录以下长期规则：

- 每个独立实验步骤都要写中文实验日志。
- 日志文件名包含日期和大致工作描述。
- 日志正文记录精确到秒的日志日期。
- 日志至少包含目的、背景、操作、结果、结论、下一步方案。
- 每次新增日志后更新 `experiment_logs/README.md` 总览表。
- 计划、方案、实验设计、执行步骤和阶段性总结使用中文。
- 写代码时添加足够中文注释，覆盖文件功能、函数功能和复杂逻辑。

## 结论

后续 agent 可以通过读取 `/home/shiyuhong/Time/AGENTS.md` 获得项目级长期协作规范。该文件比单纯依赖对话上下文更稳定，适合跨会话延续要求。

## 下一步方案

1. 后续每次开始新的实验或代码修改前，先参考 `AGENTS.md`。
2. 每完成一个独立步骤后，继续在 `experiment_logs/` 中新增日志。
3. 如果用户后续提出新的长期偏好，应继续整理为明确条款并追加到 `AGENTS.md`。
